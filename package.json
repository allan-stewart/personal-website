{
  "name": "personal-website",
  "version": "1.0.0",
  "description": "Generator for my personal website.",
  "main": "build.js",
  "dependencies": {
    "http-server": "^0.12.3",
    "marked": "^4.0.10"
  },
  "scripts": {
    "start": "node build && http-server ./www",
    "build": "node build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Allan Stewart",
  "license": "MIT",
  "private": true
}
